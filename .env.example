# Copy to .env.local for `vercel dev` (never commit real secrets).
# BVS_ADMIN_PASS_HASH_HEX must match `ADMIN_PASS_HASH_HEX` in BVS_Dashboard_v2_1.html (SHA-256 hex).
BVS_ADMIN_PASS_HASH_HEX=

# From Vercel → Storage → Upstash Redis (REST)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
